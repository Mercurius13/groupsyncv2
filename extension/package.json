{
  "name": "groupsync-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/background.ts --bundle --outfile=dist/background.js --format=esm --target=chrome110 && esbuild src/content.ts --bundle --outfile=dist/content.js --format=iife --target=chrome110 && esbuild src/inject.ts --bundle --outfile=dist/inject.js --format=iife --target=chrome110 && esbuild src/popup.ts --bundle --outfile=dist/popup.js --format=esm --target=chrome110",
    "watch": "npm run build -- --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "esbuild": "^0.21.5",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
