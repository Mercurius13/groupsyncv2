/** Must match backend/config.py's GOOGLE_CLIENT_ID (same OAuth client, set
 *  via the repo root .env for the backend) — this is the public client ID,
 *  never the client secret, which the extension must never hold. */
export const GOOGLE_OAUTH_CLIENT_ID = "1064467429480-2qtbblj3v9iv2g7no6cd56hijfufvek0.apps.googleusercontent.com";
